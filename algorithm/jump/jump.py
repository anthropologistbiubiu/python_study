from typing import List

class Solution:
    def jump(self, nums: List[int]) -> int:
        dp = [0 for _ in range(0, len(nums))]
        for index, num in enumerate(nums):
            pass


solution = Solution()
solution.jump([1, 2, 3, 4])
